"""Промпты — в одном месте и с версиями.

**Почему отдельным модулем.** Промпт — не строка интерфейса и не сообщение
человеку: его читает модель. Держать его рядом с текстами кнопок значит
однажды перевести его на узбекский вместе с ними. Держать по одному в каждом
сценарии — значит не иметь возможности прочесть их все подряд, а именно так
их и приходится читать, когда разбираешься, почему ответы поехали.

**Почему по-русски.** Промпт правит владелец системы, а не только модель.
Промпт, который некому вычитать, — чёрный ящик внутри собственного кода.

**Почему версия рядом.** Версия уходит в журнал вызовов. Изменили формулировку,
не тронув версию, — и объяснить, почему вчера разбор был другим, уже нечем.
Правило простое: меняется текст — меняется версия.
"""
from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class Prompt:
    """Указание модели и версия этого указания."""

    version: str
    system: str


VOICE_TASK = Prompt(
    version="voice-1",
    system=(
        "Ты разбираешь устное поручение руководителя. Речь бывает на узбекском "
        "(латиница или кириллица) и на русском, часто вперемешку.\n"
        "Верни только JSON, без пояснений и без разметки, с полями:\n"
        '  "title" — что нужно сделать, одной строкой, на языке оригинала;\n'
        '  "assignee" — имя или фамилия исполнителя так, как они прозвучали; '
        "пустая строка, если исполнитель не назван;\n"
        '  "due" — дословный фрагмент речи о сроке ("завтра", "juma gacha", '
        '"через три дня"). Не вычисляй дату, не переводи в другой формат, '
        "не добавляй год. Пустая строка, если срок не назван;\n"
        '  "priority" — одно из: low, normal, high, critical.\n'
        "Ничего не додумывай: чего не прозвучало, того в ответе нет."
    ),
)

# Подсказка расшифровщику о языке и лексике — тоже указание модели, а не текст
# для человека. Речь в кабинете смешанная, и без подсказки узбекские имена
# превращаются в случайные слова.
VOICE_HINT = "Oʻzbek va rus tili. Ish topshirigʻi. Рабочее поручение."


MORNING_DIGEST = Prompt(
    version="digest-1",
    system=(
        "Ты пишешь короткое вступление к утренней сводке руководителя. "
        "Всё уже посчитано до тебя; твоя работа — расставить акценты: "
        "с чего начать день, что важнее, о чём стоит помнить.\n"
        "Правила, нарушение любого делает ответ негодным:\n"
        "  * от четырёх до семи строк, каждая — законченная мысль; "
        "без списков, заголовков и разметки;\n"
        "  * ни одной цифры в ответе. Любое число подставляй меткой "
        "в фигурных скобках из списка ниже — значение впишет система;\n"
        "  * числа словами тоже не пиши: только метки;\n"
        "  * пользуйся только теми метками, что даны. Метку, которой нет "
        "в списке, писать нельзя;\n"
        "  * не советуй ничего сверх данных и не придумывай фактов: "
        "чего нет в списке, того не было;\n"
        "  * не пересказывай список по порядку — сводку с числами человек "
        "увидит следом сам."
    ),
)
