# Текущее состояние

**Обновлено:** 31 августа 2026
**Блок:** 1 из 6 — завершён и проверен
**Следующий:** блок 2 — поручения и контроль

---

## Готово

### Блок 1 — Ядро

- **Регистрация** тремя путями: ссылка отдела, свободная заявка, персональная
  одноразовая ссылка. Завершается подтверждением номера телефона кнопкой Telegram.
- **Роли и права:** 6 ролей, 28 прав, 69 связок с областями видимости.
  Проверка на уровне отдельной записи, а не только права.
- **Делегирование:** модель готова, интерфейс выдачи — в блоке 2.
- **Индикатор доступности** руководителя с автоматическим истечением и поздним приёмом.
- **Рабочее время:** 09:00–19:00, обед, буфер, выходные, поздний предел.
- **Журнал аудита:** только добавление, «было/стало», настоящий автор при делегировании.
- **Администрирование в боте:** заявки, сотрудники, отделы, ссылки-приглашения, журнал.
- **Инфраструктура:** Docker Compose, миграции Alembic, `/health`, dev-оверрайд
  с монтированием исходников.

### Проверено

`scripts/smoke_block1.py` — **26 сценариев, 0 ошибок**. Среди них:

- сотрудник НЕ открывает чужое поручение;
- начальник отдела НЕ видит соседний отдел;
- пересланная многоразовая ссылка не выдаёт роль начальника отдела;
- до подтверждения заявки прав нет вообще;
- истёкший индикатор доступности снимается сам;
- регистрация, подтверждение и переключение доступности попадают в журнал.

---

## Не сделано

- **Бот ни разу не запускался с настоящим токеном.** В `.env` подставлен
  placeholder. Владелец должен вписать `BOT_TOKEN` и `BOOTSTRAP_ADMIN_TELEGRAM_ID`.
- Сервер не арендован, домена нет. Работаем локально в режиме long polling.
- Mini App не начат (блок 5).
- ИИ не подключён (блок 6). Ключ OpenAI у владельца есть.
- Переводы на узбекский не заполнены (блок 5). Архитектура двуязычная заложена.

---

## Как запустить

```bash
cd seta
# 1. вписать BOT_TOKEN и BOOTSTRAP_ADMIN_TELEGRAM_ID в .env
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
docker compose logs -f bot
```

## Как проверить

```bash
cd seta
docker compose -f docker-compose.yml -f docker-compose.dev.yml \
  run --rm --no-deps migrate python scripts/smoke_block1.py
```

Ожидается `Пройдено: 26   Ошибок: 0`.

---

## Следующий блок: поручения и контроль

Состав:

1. Создание поручения: название, описание, исполнитель, срок, приоритет.
2. Жизненный цикл: Новое → Принято → В работе → На проверке → Выполнено,
   плюс Заблокировано / Просрочено / Отменено.
3. Проверка качества: возврат на доработку с комментарием.
4. Напоминания и эскалация по порогам (Р-10 в DECISIONS.md).
5. Запрос продления срока с обоснованием и одобрением.
6. Комментарии и файл-отчёт внутри поручения.
7. Шаблоны поручений, флаг «на личном контроле».
8. Планировщик на ARQ с распределённой блокировкой — одно напоминание уходит один раз.

**Критерий готовности:** поручение прошло полный путь на живых людях — выдано,
принято, выполнено, возвращено на доработку, переделано, принято; просрочка
эскалировала по всей цепочке без ручного вмешательства.

**Что понадобится от владельца:** ничего, кроме запущенного бота и 2–3 человек
для проверки.
